

const initialState = {
    "text": {
        id: "text",
        configurable: false
    },
    "time": {
        id: "time",
        configurable: false
    }
};


export function widgetTypes(state = initialState, action) {
    switch (state) {
        default:
            return state;
    }
}